import React from "react"
import Layout from "../components/layout";
const SitePage = () =>{
    return(
        <Layout>
            <h1>This is a test external page that is connected to the nav bar</h1>
        </Layout>
    )
    
}
export default SitePage